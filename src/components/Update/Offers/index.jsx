import { LocationName } from '../../hooks/useLocationName';
import { Container } from './styled';

export const Offers = () => {
    return (
        <Container>
            <LocationName />
        </Container>
    )
}